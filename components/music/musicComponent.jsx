import { Image, TouchableOpacity, View, Text } from "react-native";

import styles from "./musicComponent.style";


const MusicComponent = ({ iconUrl, dimension, handlePress }) => {
  return (
    <View >
        <Text>MusicComponent</Text>
    </View>
  );
};

export default MusicComponent;
